# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds an application, so they are not needed.
*.
#*.spec
# PyInstaller
build/
dist/
*.spec

# Local Database
db.sqlite3

# Python cache
__pycache__/
*.pyc
manifest
# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff
# Ensure your specific database file is listed if not sqlite3
db.sqlite3
db.sqlite3-journal # SQLite journal file
*.log
local_settings.py # Common for local overrides not to be committed
# media/ # If you don't want to commit user-uploaded media files (common)
# static_collected/ # Or whatever your STATIC_ROOT is, if you collect static files locally and don't want to commit them

# Virtual Environments
.env
.venv
env/
venv/
ENV/
VENV/
env.bak/
venv.bak/

# Conda environments
.conda/
conda-meta/

# dotenv
.env.* # For .env.local, .env.development, etc. (but .env itself is often committed if it's a template)
# If .env contains secrets, ensure it's ignored:
# .env

# IDE / Editor specific
.idea/ # PyCharm
.vscode/ # VS Code
*.project # Various IDEs
*.swp # Vim swap files
*~ # Backup files
*.sublime-project
*.sublime-workspace
.project # Eclipse
.pydevproject # PyDev

# OS generated files
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
ehthumbs.db
# Windows
Thumbs.db
desktop.ini

# Instance folder (Flask)
instance/
# If you have other sensitive files or large generated files, add them here.
# For example:
# secrets.json
# config.local.yaml
# output/
# temp/
